@using datamodel
@model IEnumerable<vBooksOfStudent>
@{
    ViewBag.Title = "Home Page";
}


<script>
    $('#js-select').select({
  ajax: {
    url: 'Student/Autocomplete',
    data: function (params) {
      var query = {
        search: params.term
      }

      // Query parameters will be ?search=[term]&type=public
      return query;
    }
  }
});
</script>

